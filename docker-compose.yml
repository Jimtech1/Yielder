version: '3.8'

services:
  yielder-backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/prod.db
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped